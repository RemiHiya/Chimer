#ifndef CHR_MACROS_H
#define CHR_MACROS_H

#define MAX(a, b)                               \
    a > b ? a : b

#define MIN(a, b)                               \
    a < b ? a : b

#endif // CHR_MACROS_H
