#include "include/chr.h"
#include "include/macro.h"
#include <stdio.h>

int main(int argc, char *argv[]) {

    if (argc < 2) {
        printf("Missing input file.");
        return 1;
    }
    return 0;
}
